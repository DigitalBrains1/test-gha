name: Test base ref

on: push

jobs:
  baseref:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Check base
        env:
          BASE_REF: ${{ github.base_ref }}
        run: ./check-base.sh
